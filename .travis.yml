language: cpp
compiler: clang++
os: osx

before_install:
  - brew update
  - HOMEBREW_NO_AUTO_UPDATE=1 brew install gmsh
  - HOMEBREW_NO_AUTO_UPDATE=1 brew install eigen
  - HOMEBREW_NO_AUTO_UPDATE=1 brew install dpkg
  - ls /usr/include
  - ls /usr/lib
  - ls /usr/local/include
  - ls /usr/local/lib
  - gmsh -version

install: skip

script:
  - make -C Assignments/A1
  - ./Assignments/A1/A1.out "TestMesh.msh"
